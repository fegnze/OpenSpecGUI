## 1. 可重复的 macOS 构建命令

- [x] 1.1 配置 Electron Packager 覆盖已有的同架构输出，并显式声明 ASAR 验证所需依赖。
- [x] 1.2 整理 arm64、x64 与完整 macOS 发布命令，确保每个架构包在成功返回前都会通过验证。

## 2. 包内容验证

- [x] 2.1 实现架构感知的发布包验证脚本，检查 `.app`、`app.asar` 及包内生产源码与当前工作区的一致性。
- [x] 2.2 为验证脚本覆盖当前包、旧包、缺失包和错误架构等成功与失败场景。

## 3. 文档与验收

- [x] 3.1 更新构建文档，明确发布命令、输出位置和包内验证成功标准。
- [x] 3.2 运行脚本测试、语法检查、架构打包验证与 OpenSpec 严格验证，并处理发现的问题。
