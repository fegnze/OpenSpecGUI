## 1. 确定性 UI 验证基础

- [x] 1.1 固定 Electron E2E fixture 的项目名称、提案状态、任务数量、文件时间和主题，消除截图中的动态时间与随机数据
- [x] 1.2 增加与现有 Playwright 版本对齐的视觉断言能力和显式基线更新脚本，保留 core 与 service 的 `node:test` 入口
- [x] 1.3 为视觉测试统一禁用非必要动画、固定字体环境和截图裁剪规则，并输出 baseline、actual 与 diff 产物
- [x] 1.4 接入自动可访问性检查，在首页、提案详情、文档阅读和打开后的项目管理对话框分别执行
- [x] 1.5 增加页面级横向溢出、关键文本截断、固定控件尺寸和主要操作可达性的 E2E 辅助断言

## 2. 视觉方向探索与确认

- [x] 2.1 使用同一套真实结构 fixture 产出“均衡密度”界面探索，覆盖首页、提案详情和文档阅读
- [x] 2.2 使用同一套 fixture 产出“高密度”界面探索，保持相同功能、内容和安全边界以便公平比较
- [x] 2.3 为两套探索生成 1440×930、1180×760、820×640 以及明暗主题截图
- [x] 2.4 固定 Taste Skill revision，对两套探索执行层级、排版、间距、密度和通用 AI 模板化审计，并记录采纳与拒绝的建议
- [x] 2.5 按首次理解、日常扫描、最小窗口、文档阅读和无障碍矩阵确认一个方向，将选定 token、布局和截图写入项目设计基线

## 3. 视觉 token 与应用框架

- [x] 3.1 将现有 CSS custom properties 整理为排版、空间、表面、语义状态和动效五组 token，并补齐明暗主题映射
- [x] 3.2 将界面字体切换为平台系统字体与中文 fallback，限制 monospace 仅用于机器 ID、任务编号和精确数字
- [x] 3.3 统一按钮、图标按钮、选择器、状态标签、进度、焦点、禁用和悬浮反馈，确保状态不只依靠颜色表达
- [x] 3.4 重构应用框架和可拖动标题栏，使宽桌面使用完整侧栏、紧凑桌面使用窄侧栏、最小桌面使用图标导航轨道
- [x] 3.5 实现最小桌面的项目切换与管理入口，并为图标化导航提供可访问名称、当前状态和悬浮说明
- [x] 3.6 统一 fast/standard 动效并验证 reduced motion 下移除非必要位移、循环和入场效果

## 4. 首页与提案集合

- [x] 4.1 将首页标题区收敛为项目上下文、刷新状态和关键摘要，移除重复英文眉题与竞争性辅助信息
- [x] 4.2 用状态筛选和单一紧凑提案列表替代首页泳道，完整展示名称、机器 ID、状态、任务进度和更新时间
- [x] 4.3 实现宽、紧凑和最小布局下的提案行信息折叠规则，保证首个相关提案位于首个支持视口内
- [x] 4.4 将无提案、空筛选结果和状态组为空收敛为不挤占主路径的紧凑状态，同时保留切换、管理和刷新入口
- [x] 4.5 统一活动提案、规范和归档集合页的筛选、列表、数量与打开反馈，避免同类信息使用不同视觉语法

## 5. 提案详情与任务执行

- [x] 5.1 重构提案详情头部，使身份、状态、完成数、总数和精确进度形成单一清晰摘要
- [x] 5.2 将当前或下一项任务实现为紧凑焦点区，支持定位到对应任务行并显示所属阶段与剩余数量
- [x] 5.3 重构任务阶段和任务行，降低已完成任务权重，突出当前与未完成任务，并稳定长文本和进度布局
- [x] 5.4 将警告、规划文档和状态来源收敛为次级上下文，在宽桌面并列、紧凑桌面下移、最小桌面受控折叠
- [x] 5.5 完成无 tasks、局部文档错误、CLI 回退和全部完成状态的非阻断视觉与键盘路径

## 6. 文档阅读与项目管理

- [x] 6.1 重构文档阅读页的标题、文档切换、受控正文宽度和页面内目录，保持中英文长文层级稳定
- [x] 6.2 为宽表格、代码块、长链接和超长机器 ID 实现局部溢出策略，阻止页面级横向滚动
- [x] 6.3 统一 Markdown 标题、列表、任务项、引用、表格、代码和行内标记的明暗主题样式
- [x] 6.4 重构项目管理对话框的项目健康状态、路径、当前项目、扫描候选和主要操作层级
- [x] 6.5 验证项目管理正常、重复导入、失效、重新关联、移除确认、扫描空结果和批量候选状态的焦点与错误反馈

## 7. 质量门禁与交付验收

- [x] 7.1 为宽桌面浅色首页/详情、紧凑详情、最小首页/详情、深色文档和项目管理关键状态建立审阅后的视觉基线
- [x] 7.2 增加仅键盘完成项目切换、状态筛选、打开详情、切换任务/文档和返回路径的 E2E 测试
- [x] 7.3 在所有关键页面运行自动可访问性检查并修复严重违规，同时完成人工焦点、VoiceOver 名称和文本放大检查
- [x] 7.4 在 1440×930、1180×760 和 820×640 下验证无控件重叠、不可达操作、异常跳动、页面级横向滚动或关键文本截断
- [x] 7.5 使用至少一个真实大型 OpenSpec 项目和一个结构差异明显的 fixture 验证高密度列表、长任务与长文档
- [x] 7.6 运行固定 revision 的 Taste 复审和 Ponytail review/lite 范围审查，修复有效问题并记录未采纳建议的理由
- [x] 7.7 运行完整测试、语法检查、依赖审计与 OpenSpec strict validation，并修复所有失败项
- [x] 7.8 构建 macOS arm64/x64 应用和 ZIP，使用打包版本复核关键基线、窗口拖动、项目切换与只读边界

## 8. 项目选择与提案导航修正

- [x] 8.1 用应用内 listbox 项目菜单替换侧栏原生下拉，提供清晰选中态、健康状态、较大命中区和完整键盘关闭路径
- [x] 8.2 移除“提案运行”独立主导航，将旧 changes 路由和详情返回路径归一到执行台
- [x] 8.3 将“需要处理”统一排列在“待归档”之后，并让顶部状态摘要直接筛选执行台提案列表
- [x] 8.4 更新 Electron E2E 覆盖项目菜单键盘切换、重复导航移除、摘要筛选与列表标题同步
- [x] 8.5 审阅并更新受影响视觉基线，运行完整测试、可访问性检查和 OpenSpec strict validation

## 9. 执行台首页语义纠偏

- [x] 9.1 明确保留完整执行台首页，删除的仅是“提案运行”独立导航，并补充默认首页恢复场景
- [x] 9.2 在显式进入执行台、切换项目和旧 `changes` 路由归一时清除临时状态筛选
- [x] 9.3 增加执行台首页恢复的 Electron E2E 回归并运行完整测试与 OpenSpec strict validation

## 10. 恢复原执行台状态总览

- [x] 10.1 撤销“全量提案表格作为默认首页”的错误决策，明确状态总览与聚焦列表的双层交互
- [x] 10.2 恢复进行中、待归档、需要处理三组执行台提案卡片，并让顶部摘要按需切换聚焦列表
- [x] 10.3 更新 Electron E2E、响应式与视觉基线，运行完整测试、打包验证和 OpenSpec strict validation

## 11. OpenSpec GUI 专属应用图标

- [x] 11.1 设计无文字的“规范文档 + 执行提示符”SVG 母版，并审阅 1024px、128px、32px 与 16px 输出
- [x] 11.2 生成 PNG/ICNS，接入侧栏品牌位、macOS Dock、Electron Forge 与 arm64/x64 应用包
- [x] 11.3 增加图标资产与打包配置回归，更新视觉基线并运行完整测试、打包验证和 OpenSpec strict validation

## 12. 替换为用户指定图标

- [x] 12.1 将用户提供的 `SPEC` 齿轮徽章保存为项目图标母版，并确定性生成 1024px renderer PNG、Dock PNG 与 ICNS
- [x] 12.2 将侧栏、开发态 Dock、Electron Forge 和图标回归测试切换到新资产，移除上一版活动 SVG 及其专用渲染脚本
- [x] 12.3 审阅 1024px、128px、32px、16px 和关键界面视觉结果，更新基线并完成测试、arm64/x64 打包与 strict validation

## 13. 分离 APPICON 与内部产品标识

- [x] 13.1 将第一张用户图保存为 1024px APPICON 母版并生成 Dock PNG 与 ICNS，将第二张透明 PNG 保存为独立内部产品标识
- [x] 13.2 将侧栏和 E2E 切换到内部产品标识，移除 renderer 中的 APPICON 副本，并扩展资产回归以验证两张源图不混用
- [x] 13.3 审阅关键界面视觉结果，更新基线并完成完整测试、arm64/x64 打包、包内哈希校验和 strict validation

## 14. 修复 APPICON 透明边缘

- [x] 14.1 为 APPICON 构建链增加可复现的抗锯齿圆角 Alpha 蒙版，只移除金属底板外的假棋盘格
- [x] 14.2 扩展资产回归，验证 PNG 为 RGBA、四角透明且中央与底板主体保持不透明
- [x] 14.3 审阅多尺寸输出并完成测试、arm64/x64 打包、包内哈希校验和 strict validation
