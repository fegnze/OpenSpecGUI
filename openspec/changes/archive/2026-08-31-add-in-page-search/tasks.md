## 1. 页内查找交互

- [x] 1.1 在 renderer 添加可访问的页内查找栏、匹配计数、前后导航和关闭控件，并完成明暗主题与紧凑窗口样式。
- [x] 1.2 实现 `⌘F` / `Ctrl+F`、Enter、Shift+Enter 和 Escape 键盘操作，同时保留 `/` 对全局搜索的既有行为。

## 2. 匹配与生命周期管理

- [x] 2.1 实现仅针对当前主内容区的不区分大小写匹配、全部结果高亮、当前结果定位和循环导航。
- [x] 2.2 在清空、关闭、路由切换和重新渲染时可靠地移除页内查找状态与高亮，不影响已渲染 Markdown 内容。

## 3. 验证

- [x] 3.1 为快捷键、匹配计数、前后循环和关闭/重渲染清理补充 renderer 或端到端测试。
- [x] 3.2 运行语法检查、相关测试和 OpenSpec 严格验证，并处理发现的问题。
