## 1. 悬浮侧栏布局

- [x] 1.1 在 `src/renderer/styles.css` 为宽桌面外壳与侧栏定义一致的间距、圆角、边界和低对比层次样式。
- [x] 1.2 保留侧栏固定定位、顶部窗口控制区安全留白，以及侧栏和工具栏的拖拽与交互边界。

## 2. 响应式回退

- [x] 2.1 调整现有桌面断点，使紧凑和最小窗口继续采用图标优先侧栏，并避免横向溢出、控件重叠或正文过度压缩。
- [x] 2.2 检查并修正悬浮容器中的键盘焦点轮廓、底部状态和长内容滚动边界。

## 3. 验证

- [x] 3.1 更新受影响的静态或视觉回归断言，覆盖宽桌面悬浮侧栏和紧凑窗口回退行为。
- [x] 3.2 在宽桌面、紧凑桌面和最小窗口尺寸手动验证窗口控制区避让、拖拽、导航、滚动和可读性。
- [x] 3.3 运行 `npm run check`、`npm test`、`npm run test:visual` 和 `openspec validate float-sidebar-layout --strict`。

## 4. 真实窗口缺陷修正

- [x] 4.1 收窄宽桌面外壳留白、列间距和圆角，使悬浮层级保持可见但不过重。
- [x] 4.2 增加与侧栏对齐的独立标题栏拖拽区，并以静态和端到端断言保护其拖拽语义与工作区边界。
- [x] 4.3 更新视觉基线，运行全量验证并重新构建 arm64 与 x64 应用包。

## 5. 单侧栏悬浮收敛

- [x] 5.1 移除工作区的圆角、边框与外壳留白，仅保留侧栏悬浮表面。
- [x] 5.2 调整拖拽区和布局回归断言，使其与新的单侧栏边界一致。
- [x] 5.3 更新视觉基线并运行完整验证与 macOS 双架构重建。

## 6. 细化侧栏外沿

- [x] 6.1 将侧栏外边距收窄至 4px，并降低面板边框对比度。
- [x] 6.2 更新视觉基线，运行完整验证并重新构建 macOS 双架构包。

## 7. 移除无语义顶部装饰

- [x] 7.1 移除侧栏顶部绿色装饰条及其响应式覆盖规则。
- [x] 7.2 更新视觉基线，运行完整验证并重新构建 macOS 双架构包。

## 8. 贴边圆角双面板收敛

- [x] 8.1 将侧栏外边距收至 0px，令侧栏与工作区无缝衔接，并为工作区补上与侧栏一致的内缘圆角。
- [x] 8.2 更新布局回归断言，覆盖零外沿间距、相邻圆角和标题栏拖拽边界。
- [x] 8.3 更新视觉基线，运行完整验证并重新构建 macOS 双架构包。

## 9. 顶栏圆角收口

- [x] 9.1 为宽桌面工作区顶栏增加显式顶部圆角，并裁剪粘性内容到工作区圆角轮廓内。
- [x] 9.2 更新回归断言和视觉基线，验证宽桌面圆角与紧凑窗口直角回退。
- [x] 9.3 运行完整验证并重新构建 macOS 双架构包。

## 10. 侧栏 2px 外沿收敛

- [x] 10.1 将宽桌面侧栏左、上、下外沿设为 2px，恢复四角圆角，并保持与工作区的零缝衔接及拖拽边界。
- [x] 10.2 更新布局回归断言和视觉基线，覆盖 2px 外沿、圆角和紧凑窗口回退。
- [x] 10.3 运行完整验证并重新构建 macOS 双架构包。

## 11. 双面板间距收敛

- [x] 11.1 在宽桌面侧栏与工作区之间建立 4px 间距，并让标题栏拖拽区止于侧栏边缘。
- [x] 11.2 更新布局回归断言和视觉基线，覆盖面板间距、圆角和紧凑窗口回退。
- [x] 11.3 运行完整验证并重新构建 macOS 双架构包。

## 12. 最终外沿与间距收敛

- [x] 12.1 将宽桌面侧栏外沿收至 0px、面板间距设为 6px，并保持完整圆角及拖拽边界。
- [x] 12.2 更新布局回归断言和视觉基线，覆盖 0px 外沿、6px 间距和紧凑窗口回退。
- [x] 12.3 运行完整验证并重新构建 macOS 双架构包。
