## 1. 桌面应用骨架

- [x] 1.1 创建 `package.json`、Node 版本约束、开发/测试/检查/打包脚本和基础 `.gitignore`
- [x] 1.2 配置 Electron Forge 与 macOS arm64/x64 构建目标，并建立 `src/main`、`src/preload`、`src/core`、`src/renderer` 和 `test` 目录
- [x] 1.3 实现 Electron 生命周期、单实例锁、主窗口最小尺寸和恢复已有窗口行为
- [x] 1.4 使用本地自定义协议加载 renderer 资源，并配置严格 CSP 与安全窗口选项
- [x] 1.5 建立 preload 的 `window.openSpecGUI` 窄接口及共享的请求/响应数据约定

## 2. OpenSpec 读取核心迁移

- [x] 2.1 从单项目原型迁移 `workspace.js`、`markdown.js`、`openspec-cli.js` 及可复用单元测试，不迁移 HTTP server 和旧 CLI 入口
- [x] 2.2 将工作区解析入口改为接收显式 `projectContext`，并保持现有提案状态、tasks、specs 和文档输出语义
- [x] 2.3 实现以当前 `openspecPath` 为边界的文档路径解析与目录穿越拒绝逻辑
- [x] 2.4 实现 Markdown 结构化渲染和白名单清洗，阻止脚本、事件属性、危险 URL 与未授权外部资源
- [x] 2.5 实现项目本地、显式配置、随包候选和 GUI `PATH` 的 CLI 多级解析
- [x] 2.6 为 CLI 调用增加固定项目 cwd、参数数组、超时、输出上限、结构校验和文件解析回退
- [x] 2.7 补充核心测试，覆盖活动/归档提案、缺失 artifacts、CLI 失败、路径越界和恶意 Markdown

## 3. 项目注册与发现

- [x] 3.1 实现版本化项目注册表模型、UUID 身份、真实路径去重和有效性状态
- [x] 3.2 在应用用户数据目录实现注册表加载、临时文件原子保存、完整版本恢复和错误诊断
- [x] 3.3 实现单项目目录选择、直接 `openspec` 目录验证、重复导入复用与自动选中
- [x] 3.4 实现父目录有界扫描、排除目录、符号链接跳过、候选结果与取消控制
- [x] 3.5 实现候选项目批量确认导入，并保证扫描阶段不提前修改注册表
- [x] 3.6 实现项目切换、最近打开时间更新和当前项目回退规则
- [x] 3.7 实现项目移除，验证其仅修改注册表且不改动源项目文件
- [x] 3.8 实现失效项目标识和重新关联，保留原项目 ID 并更新规范化路径
- [x] 3.9 补充注册表与发现测试，覆盖原子写入中断、重复路径、大目录排除、失效路径和重新关联

## 4. 主进程服务与 IPC

- [x] 4.1 注册 `projects:list/add/scan/select/remove/relink` IPC handler，并校验所有输入
- [x] 4.2 实现当前项目工作区快照、`projectId + revision` 标识和仅索引当前项目的缓存策略
- [x] 4.3 注册 `workspace:load/refresh` 与 `documents:read` handler，并确保响应不泄露任意文件能力
- [x] 4.4 实现项目切换和刷新时的 revision 失效机制，阻止旧异步结果覆盖新项目
- [x] 4.5 实现窗口重新获得焦点时的轻量变更检测，只有 OpenSpec 内容变化时才重建快照
- [x] 4.6 实现受限 `clipboard:write`，并验证 renderer 无法直接获得 Node、Electron 或任意 IPC 接口
- [x] 4.7 补充 IPC 集成测试，覆盖无当前项目、非法 ID、路径越界、并发切换和局部读取错误

## 5. 工作台界面迁移

- [x] 5.1 迁移现有 renderer 的语义结构、图标、样式和本地静态资源，并将 `/api/*` fetch 替换为 preload API
- [x] 5.2 实现首次启动空状态、添加项目和扫描目录的原生入口
- [x] 5.3 在侧栏顶部实现常驻项目切换器、失效状态标识和紧凑的项目管理入口
- [x] 5.4 实现项目管理视图，支持候选确认、移除确认和路径重新关联
- [x] 5.5 迁移提案状态首页，保持按状态组织且不展示外置待办或跨提案任务列表
- [x] 5.6 迁移提案详情并将任务总览、精确进度和章节化 task 列表置于首要区域
- [x] 5.7 迁移 proposal、design、specs 和其他文档导航与 HTML 阅读视图
- [x] 5.8 实现项目切换时清空旧选择、搜索和文档状态，并忽略旧 revision 响应
- [x] 5.9 实现 CLI 文件回退、文档错误、空提案和任务未生成等非阻断状态

## 6. 视觉与交互验收

- [x] 6.1 统一现代中英文字体栈、常规正文字重、可读字号和紧凑间距，移除密集黑粗体表现
- [x] 6.2 实现固定 10 段且支持末段部分填充的进度组件，并为 0%、83%、91% 和 100% 添加测试
- [x] 6.3 为项目切换、刷新、添加、扫描、移除和重新关联使用清晰图标、菜单、对话框与悬浮说明
- [x] 6.4 验证常用桌面尺寸和最小窗口下无文字截断、控件重叠、布局跳动或不可达操作
- [x] 6.5 使用仓库内中性任务 fixture 验证任务优先层级和视觉密度没有回退

## 7. 端到端与打包验证

- [x] 7.1 增加覆盖首次启动、导入、扫描、切换、刷新、移除和重新关联的端到端测试
- [x] 7.2 使用两个结构不同的仓库内合成 OpenSpec 项目验证 CLI 与文件回退路径
- [x] 7.3 验证应用不监听 HTTP 端口、第二次启动复用已有实例、被导入项目保持无文件变更
- [x] 7.4 运行完整单元测试、集成测试、静态检查和 OpenSpec 严格校验并修复失败项
- [x] 7.5 构建 macOS arm64/x64 应用产物并对打包版本重复项目导入与切换验收

## 8. 文档与任务恢复

- [x] 8.1 编写 README，说明安装、开发、测试、打包、项目数据位置和只读边界
- [x] 8.2 在新仓库添加任务恢复提示词，记录原始实现路径、当前 OpenSpec change、架构边界、已完成项、验证命令和继续实施方式
- [x] 8.3 记录原型到桌面应用的迁移验收结果，并明确后续任务只依赖本仓库规范和测试
